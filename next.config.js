/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  compiler: {
    styledComponents: true,    // config to enable styled component in nextJS
  },
};

module.exports = nextConfig;
